[package]
name = "treasury_vault"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["lib", "cdylib"]

[dependencies]
alloy-primitives = "0.8"
alloy-sol-types  = "0.8"
mini-alloc       = "0.4.2"
stylus-sdk       = "0.8"
hex              = "0.4.3"

[features]
export-abi = ["stylus-sdk/export-abi"]

[profile.release]
codegen-units = 1
strip = true
lto = true
panic = "abort"
opt-level = 3
